# Открытые вопросы

Не додумывать при кодировании — уточнять у заказчика/методичек и фиксировать решение здесь же, рядом с вопросом, а не только в чате. **Для ПЗ1 в первую очередь сверяться с `ТЗ v3.3`/`v3.4`/`v3.5`** — они точнее и новее этого файла.

## 🔴 Блокеры — не трогать код до ответа заказчика (по v3.5)

1. **Формула итогового времени хода — возможен двойной учёт разгона/торможения.** Текущий расчёт 01:13 (построчно верен), заказчик хочет 01:18 (получается только при повторном прибавлении 3×перегонов поверх уже посчитанного). Показать заказчику раскладку, не менять формулу самостоятельно. См. `pz-specs.md`, ПЗ1.
2. **Формула стоимости личного авто — чьи «Прочие параметры» использовать**, если у двух станций корреспонденции разные значения (актуально с тех пор, как «Прочие параметры» стали по станциям, не по корреспонденциям).
3. **Формула стоимости личного авто — какая длина трассы.** Общая длина всей линии, или длина конкретного перегона/корреспонденции? Существенно при >2 станций.
4. **Аннотация «по умолчанию существующая + 200»** — неясно, к какому полю относится (стрелка на слайде визуально пересекается с несколькими полями).

## Не решено, требует вердикта заказчика — высокий приоритет, не блокер

5. **Модель карты ПЗ1.** Заказчик предложил альтернативу «два независимых набора точек»: сначала трасса, точка на трассе назначается станцией. Ответ на присланное текстовое объяснение — статус неизвестен, проверить.
6. **13 итоговых показателей ПЗ1, похоже, не связаны с расчётами** — свободный ручной ввод вместо вычисляемых полей (билетная выручка положительная при нулевом потоке — прямое свидетельство). Самый крупный оставшийся объём работы по ПЗ1, требует отдельного проектирования «что из чего считается» — не начинать без плана. См. `pz-specs.md`.
7. Кнопки режимов карты всё ещё не в одну строку, несмотря на более раннее требование — проверить, не потерялось ли при последующей вёрстке
8. Warning-валидация скорости перегона (вне 50–400 км/ч) не проявляется видимо — проверить, реализована ли
9. Функция удаления вида транспорта из корреспонденции — регрессировала, проверить git-историю компонента перед переписыванием с нуля
10. Округление длины трассы расходится между экраном и PDF — унифицировать на 2 знака

## Требуют заказчика/преподавателя (не срочно)

11. Режим вставки Web Object в iSpring — «на слайде» или «новое окно»
12. Поддержка auto-resize через `postMessage` плеером iSpring
13. Тёмная тема — нужна ли вообще
14. ПЗ5 — топология станции одна на всех команд или различается по вариантам
15. ПЗ7, часть 2 — точная механика «увязки поездов по станциям оборота»
16. ПЗ8 — трактовка «конструктор анкеты + импорт результатов внешнего опроса»
17. Полигоны регионов на карте ПЗ1 — требование методички или артефакт Яндекс.Карт

## Требует уточнения по методичке/модели

**Роль строки 9 «Коэффициент эластичности к уровню комфорта» в таблице дискомфорта.** Всегда равна 1 во всех предзаполненных данных. В агрегат (среднее строк 1–8) не входит. Не ясно: это множитель, применяемый к агрегату где-то дальше по модели, отдельный самостоятельный параметр, или зарезервированное на будущее поле без текущего использования. Не додумывать — сверить с `pz1-passenger-flow.md`/методичкой; если применения не находится, явно задокументировать как «зарезервировано, не используется» вместо того, чтобы оставлять вопрос без ответа при каждой пересборке документов.

## Наши архитектурные решения (пока не приняты)

18. `PzNResult` — заводить все 8 типов-заготовок сразу или по факту разработки модуля
19. Drag-and-drop импорт JSON на старте модуля — нужен ли начиная со второго ПЗ
20. PDF — момент рендера формул/диаграмм в PNG (при завершении шага vs при экспорте)
21. PDF — нужен ли отдельный титульный лист для комбинированного отчёта всей линии

## Резолвлено (не переоткрывать без новых оснований)

- ~~Персистентность данных студента~~ — **решение заказчика, дословно:** «прогресс должен подписываться из json файла и сохраняться туда же в любой момент работы через кнопку, и должен будет открываться тот же экран (задание), на котором json был сохранён с уже введёнными значениями (если задание уже было начато). Никаких Google Forms и т.д.» Только JSON-файл — не localStorage, не сервер, не загрузка по ID. См. `architecture.md`, раздел «Мост и персистентность» — там же зафиксирована структура с полем `progress` для восстановления на середине модуля (`schemaVersion` поднят до `1.1`).
- ~~«`DraftStorage`/«производные — только селекторы» — расхождение с `architecture.md`»~~ — снято: `DraftStorage` из `ТЗ v3.x` — это то же самое, что `bridge/io.ts` + typed `BridgeSchema` в `architecture.md`, просто из другого документа с другим названием, не отдельная не описанная сущность. «Производные — только селекторы» — существующее правило «Живой итог» из `architecture.md`, тоже не новое понятие. Терминология сведена в `architecture.md` явно, отдельно как открытый вопрос не тянуть.

- ~~Цветовая палитра~~ — утверждена заказчиком (`SCHOOLVSMCOLOURS.svg`)
- ~~ПЗ3 — откуда скорость участка~~ — из ПЗ1, не ПЗ2
- ~~ПЗ7↔ПЗ1 — несовпадение числа станций~~ — не противоречие, шахматка динамического размера
- ~~ПЗ6 — приоритет данных моста vs собственного варианта~~ — у ПЗ6 своя вариантная таблица
- ~~ПЗ4 — автономен ли~~ — да, но не «только паспорт» — своя вариантная таблица
- ~~Число Web Object~~ — 8 отдельных, один билд, роутинг `?pz=N`
- ~~Число шагов ПЗ1~~ — фиксированная структура «страница на тему», ≈10 шагов, подтверждено на практике (было 46 по предыдущей формуле — заказчик отклонил, переверстано)
- ~~Связь ПЗ1→ПЗ2 через радиусы кривых~~ — снято, поле хранит стрелу прогиба (`bendM`), не радиус, нормативы неприменимы
- ~~Округление радиуса~~ — целое ≥0, метры
- ~~Термин «гравитационная модель»~~ — в UI/PDF заменено на «имитационная», в коде остаётся gravity
- ~~Esc → какой режим карты~~ — **закрыто по v3.5:** Esc → «Просмотр» (решение от 11.08 про «Станция» окончательно отменено, это не гипотеза по дате, а прямое указание v3.5)
- ~~График модели не строился~~ — исправлено, подтверждено скриншотами 20.08
- ~~PDF не скачивался~~ — исправлено
